# Full backend URL including the /api prefix.
# Leave unset in local dev — the Vite proxy handles /api → http://localhost:3000.
# Required in production (Vercel). Example:
#   VITE_API_URL=https://your-backend.up.railway.app/api
VITE_API_URL=
